import { LifecyclePlaceholder } from '@/components/lifecycle-placeholder';

export default function ProgramBuildingScreen() {
  return (
    <LifecyclePlaceholder
      eyebrow="PROGRAM BUILDING"
      title="Your plan is taking shape."
      body="FLYNT is building your first week from the information you reviewed. You can leave the app and return here later."
    />
  );
}
